{% extends 'base.html.twig' %}

 {% block stylesheets %}
 {% endblock %}

{% block body %}
    <div class="wrapper">
        <div class="header clearfix">
            <div class="left"><a href="/"><div id="logo">CONSSO.FR</div></a></div>
            <div class="right">
                <ul id="navigation" class="clearfix">
                    {% if client is defined %}<a href="/logout"><li>Se déconnecter</li></a>{% endif %}
                    <a href=""><li>A propos</li></a>
                    <a href=""><li>Références</li></a>
                    <a href=""><li>Objectifs</li></a>
                    <a href=""><li>Contact</li></a>
                </ul>
            </div>
        </div>

        <div class="content ">
            <div class="block ">
                <div class="l2-block tool">
                    <div id="show" class="in-block left">
                        <h1>{% if not client.hasCompleted %}<i class="material-icons">warning</i>{% endif %}Mon bien</h1>
                        <div class="bien">
                            <p id="p1">{{ client.surface }} m²</p>
                            <p id="p2">{{ client.piece }} pièces</p>
                        </div>
                    </div>
                    <a href="{{ path('configuration') }}" >
                        <div id="mask_tool"  class="in-block left">
                            <p><h1> > Modifier</h1></p>
                            {% if not client.hasCompleted %}<p>Il semblerait que vous n'avez pas encore compléter votre bien.</p>{% endif %}
                        </div>
                    </a>
                </div>
                <div class="l6-block advice">
                    <div class="in-block right">
                        <h1>Nos conseils</h1>
                        <p>
                            <ul>
                            {% set i = 0 %}
                            {% for tip in tips if i<2 %}
                                <li>
                                   {{ tip.description }}
                                </li>
                                {% set i = i+1 %}
                            {% endfor %}
                            </ul>
                        </p>
                    </div>
                    <div id="mask_advice"  class="in-block right">
                        <ul>
                            <li>
                                <a href="/les-bons-gestes"><h2> > Accéder à tous nos conseils</h2></a>

                            </li>

                        </ul>
                    </div>
                </div>
            </div>
            <div class="clearfix"></div>
            <div class="block ">
                <div class="l2-block consommation">
                    <div class="in-block left">
                        <h1>Ma Conso.</h1>
                        <p class="conso-price">{{ consommation }}€</p>
                    </div>
                    <div id="mask_consommation"  class="in-block left">
                        {% if client.hasVoted is null %}
                            <p>Il s'agit d'une estimation basée sur votre configuration située plus haut.</p>
                        {% else %}
                            {% if client.hasVoted == 0 %}
                                <p>Vous avez signalé une mauvaise estimation. Nous nous excusons et vous remercions d'avoir participé à l'évolution de notre produit.</p>
                            {% else %}
                                <p>La dernière estimation était bonne. Nous vous remercions d'avoir participé à l'évolution de notre produit.</p>
                            {% endif %}
                        {% endif %}

                        <center><p>Notre estimation est-elle correct ? (à 2€ près)</p>
                        <a href="{{ path('estimate', {code: client.name, response: 'true'}) }}">> Oui</a>
                        <a href="{{ path('estimate', {code: client.name, response: 'false'}) }}">Non <</a></center>
                    </div>
                </div>
                <div class="l6-block company">
                    <div class="in-block right">
                        <h1>Vos consommation anormales</h1>
                        {% set alert = 0 %}
                        {% for room in rooms %}
                            {% if room.name == 'Cuisine' and room.total>=5.9 %}
                                <p>Attention ! Votre cuisine semble consommer plus que la normale.</p>
                                {% set alert = alert + 1 %}
                            {% elseif room.name == 'Salle de bain' and room.total>=2.5 %}
                                <p>Attention ! Votre salle de bain semble consommer plus que la normale.</p>
                                {% set alert = alert + 1 %}
                            {% elseif room.name == 'Salon' and room.total>=2 %}
                                <p>Attention ! Votre salon semble consommer plus que la normale.</p>
                                {% set alert = alert + 1 %}
                            {% elseif room.name == 'Chambre parentale' and room.total>=0.75 %}
                                <p>Attention ! Votre chambre parentale semble consommer plus que la normale.</p>
                                {% set alert = alert + 1 %}
                            {% elseif room.name == 'Chambre enfant' and room.total>=0.75 %}
                                <p>Attention ! Votre chambre d'enfant semble consommer plus que la normale.</p>
                                {% set alert = alert + 1 %}
                            {% endif %}
                        {% endfor %}
                        {% if alert == 0 %}
                            <p>Aucune consommation anormale, félicitation !</p>
                        {% endif %}
                    </div>
                </div>
                <div class="clearfix"></div>
            </div>
            <div class="clearfix"></div>
        </div>
        <div class="clearfix"></div>
        <div class="footer">
            Vous souhaitez consulter ou modifier des informations ultérieurement? Voici votre nouveau code : {{ client.name }}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {% javascripts'@AppBundle/Resources/public/js/*' %}<script src="{{ asset_url }}"></script>{% endjavascripts %}
    <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
    <script type="text/javascript">
        $(".tool").hover(function () {
            $("#mask_tool").toggle('slide','left',400);
        })

        $(".advice").hover(function () {
            $("#mask_advice").toggle('slide','left',400);
        })

        $(".consommation").hover(function () {
            $("#mask_consommation").toggle('slide','left',400);
        })
    </script>
{% endblock %}